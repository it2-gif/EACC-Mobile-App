org.gradle.jvmargs=-Xmx1G -Xss512k -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=128m -XX:+UseSerialGC -XX:ActiveProcessorCount=2
org.gradle.workers.max=1
org.gradle.parallel=false
org.gradle.daemon=false
org.gradle.vfs.watch=false
kotlin.compiler.execution.strategy=in-process
kotlin.daemon.jvmargs=-Xmx512m
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Current Flutter plugins use the Kotlin Gradle plugin supported by AGP 8.
android.builtInKotlin=false
# Disable Kotlin incremental compilation to avoid cache/root mismatches when
# the Flutter pub cache and project live on different drives.
kotlin.incremental=false
